apiVersion: v1
kind: Service
metadata:
  name: quick-k8s-manager-service
spec:
  selector:
    app: quick-k8s-manager
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
